# for local development
version: '3.7'
networks:
  network-custom:
    name: tunnel_nbr
    external: true
services:
  bacuav-admin:
    container_name: bacuav-admin-prod
    build:
      context: .
    ports:
      - "${NGINX_HOST_PORT}:80"
    networks:
      - network-custom
