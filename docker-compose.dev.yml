# for local development
version: '3.7'
networks:
  network-custom:
    name: tunnel_nbr
    external: true
services:
  dev-bacuav-web:
    container_name: dev-bacuav-web
    build:
      context: .
    ports:
      - "${NGINX_HOST_PORT}:80"
    networks:
      - network-custom
